# Web UI
streamlit>=1.30

# Core data processing
pandas>=2.0
openpyxl>=3.1
numpy>=1.24

# PDF extraction (pdfplumber preferred; PyPDF2 is fallback)
pdfplumber>=0.10
PyPDF2>=3.0

# LLM – cloud
openai>=1.0
anthropic>=0.40

# LLM – local (Ollama uses plain requests; no extra package needed)
requests>=2.31

# Environment / config
python-dotenv>=1.0

# Optional: OCR for scanned PDFs
# pytesseract>=0.3
# Pillow>=10.0
